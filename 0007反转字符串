//算法

//反转整数的方法可以与反转字符串进行类比。
//要在没有辅助堆栈 / 数组的帮助下 “弹出” 和 “推入” 数字，我们可以使用数学方法。

public int reverse(int x) {
        int y = 0;
        while (x != 0) {
            if (y > 214748364 || y < -214748364) {     //溢出了可以正常比较大小
                return 0;
            }
            y = y * 10 + x % 10;            //通过数学方法%和/实现数字从后往前输出。
            x = x / 10;
        }
        return y;
    }
